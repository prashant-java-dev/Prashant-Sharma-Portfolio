server:
  port: ${PORT:8080}

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:${MONGO_URI:mongodb://localhost:27017/portfolio}}
      database: ${MONGODB_DATABASE:portfolio}
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:placeholder}
    password: ${MAIL_PASSWORD:placeholder}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com
          connectiontimeout: 30000
          timeout: 30000
          writetimeout: 30000
        debug: true

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:AnyStrongSecretKey123456}
  expiration: ${JWT_EXPIRATION:86400000}

# OAuth Configuration
google:
  client-id: ${GOOGLE_CLIENT_ID:placeholder}
  client-secret: ${GOOGLE_CLIENT_SECRET:placeholder}

# CORS Configuration
app:
  cors:
    allowed-origins: ${ALLOWED_ORIGINS:http://localhost:3000}
  mail:
    from: ${MAIL_FROM:${MAIL_USERNAME:your-email@gmail.com}}
    to: ${MAIL_TO:prashantsharma3018@gmail.com}

logging:
  level:
    root: ${LOGGING_LEVEL:INFO}
    com.prashant.portfolio: ${LOGGING_LEVEL:INFO}

